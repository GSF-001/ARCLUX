# ARCLUX Security Analysis

This package provides defensive, source-based security findings and provenance
contracts. `analyzeRepositorySecurity(repository)` is the integration point for
the ARCLUX engine. It does not execute repository code or perform network scans.
